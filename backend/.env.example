# Agentic Social Manager - Backend Environment Variables

# IMPORTANT: DO NOT COMMIT SECRETS TO THE REPOSITORY.
# This example file contains placeholders only. Replace values in a local
# `.env` file (which is gitignored) with real credentials.

# Gemini API Key (for caption and hashtag generation)
GEMINI_API_KEY=REPLACE_WITH_YOUR_GEMINI_API_KEY

# Bytez API Key (for image generation with Stable Diffusion SDXL)
BYTEZ_API_KEY=REPLACE_WITH_YOUR_BYTEZ_API_KEY

# Bytez Model (optional - defaults to stabilityai/stable-diffusion-xl-base-1.0)
BYTEZ_MODEL=stabilityai/stable-diffusion-xl-base-1.0

# MongoDB Connection (Optional - for database)
MONGODB_URI=mongodb://localhost:27017

# JWT Secret (For authentication)
JWT_SECRET=your_jwt_secret_key_here

# FastAPI Host and Port
FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=8000
